 <div class="container">
    <h1 class="my-4">Books</h1>
    	<div class="row">
    	<% @books.each do |book| %>
      		<%= render "book_row", book: book, order_item: @order_item %>
    	<% end %>
    	</div>
</div>